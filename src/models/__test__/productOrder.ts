import ProductOrderModel from '../productOrder';

describe('Test productOrder Model Methods', () => {
  it('add product to order', () => {
    expect(ProductOrderModel.create).toBeDefined();
  });
});
